<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Verify Logs - Event Logging System</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="container-fluid">
            <a class="navbar-brand" href="/">Log Chain</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ms-auto">
                    <li class="nav-item"><a class="nav-link" href="/">Dashboard</a></li>
                    <li class="nav-item"><a class="nav-link" href="/submit-log">Log Event</a></li>
                    <li class="nav-item"><a class="nav-link" href="/query-log">Query Logs</a></li>
                    <li class="nav-item"><a class="nav-link" href="/verify-logs">Verify Logs</a></li>
                </ul>
            </div>
        </div>
    </nav>

    <div class="container mt-4">
        <h1 class="text-center mb-4">Verify Logs</h1>
        <% if (isValid) { %>
            <div class="alert alert-success text-center">
                <h4>✅ Logs are valid!</h4>
                <p>All logs have maintained their integrity.</p>
            </div>
        <% } else { %>
            <div class="alert alert-danger text-center">
                <h4>❌ Log Chain Broken!</h4>
                <p>Some logs have been tampered with or are inconsistent.</p>
            </div>
        <% } %>
    </div>

    <footer class="text-center py-3 bg-dark text-light">
        © 2024 Event Logging System | All Rights Reserved
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
